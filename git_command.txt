1. 设置用户身份信息用于提交：git config --global user.email “邮箱”         git config --global user.name "用户名"
2. 新建本地git仓库：git init
3. 通过SSH登录github，创建SSH，将公钥放到github。电脑需要连接github时，github通过公钥发送加密内容，电脑通过私钥解密。
4. 关联本地仓库和github仓库：git remote add origin <server>
5. 查看本地仓库关联的github仓库：git remote -v

